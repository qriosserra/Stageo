{% macro button(
    text,
    icon,
    url,
    class) %}
    <a href="{{ url }}" class="button-ghost {{ class }}">
        <i class="fi {{ icon }}"></i>
        <span>{{ text }}</span>
    </a>
{% endmacro button %}